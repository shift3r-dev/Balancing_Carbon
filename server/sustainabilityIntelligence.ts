export const roundTo = (value:number,digits=2)=>Number(value.toFixed(digits));
export function marginalAbatementCost(input:{capex:number;annualSavings:number;annualReduction:number;lifetimeYears:number}){if(input.annualReduction<=0||input.lifetimeYears<=0)return null;return roundTo(((input.capex/input.lifetimeYears)-input.annualSavings)/input.annualReduction,2);}
export function netZeroTrajectory(input:{baselineYear:number;targetYear:number;baseline:number;residual:number}){if(input.targetYear<=input.baselineYear)throw new Error('Target year must be after baseline year.');if(input.baseline<0||input.residual<0||input.residual>input.baseline)throw new Error('Pathway emissions values are invalid.');return Array.from({length:input.targetYear-input.baselineYear+1},(_,index)=>{const year=input.baselineYear+index,progress=index/(input.targetYear-input.baselineYear);return{year,target:roundTo(input.baseline-(input.baseline-input.residual)*progress,3)};});}
export function supplierScore(input:{dataQuality:string;engagement:string;renewableShare?:number|null;hasEmissions:boolean;hasEvidence:boolean}){const quality:{[key:string]:number}={unknown:0,estimated:8,'supplier-reported':16,'third-party-verified':25},engagement:{[key:string]:number}={'not-contacted':0,requested:6,responded:12,'improvement-plan':17,verified:20};const score=(quality[input.dataQuality]??0)+(engagement[input.engagement]??0)+(input.hasEmissions?20:0)+(input.hasEvidence?15:0)+Math.min(20,Math.max(0,Number(input.renewableShare??0)/5));return roundTo(score,1);}
export function targetProgress(input:{baseline:number;target:number;current:number;direction:'decrease'|'increase'}){const denominator=Math.abs(input.target-input.baseline);if(!denominator)return input.current===input.target?100:0;const achieved=input.direction==='decrease'?input.baseline-input.current:input.current-input.baseline;return roundTo(Math.max(0,Math.min(100,achieved/denominator*100)),1);}
