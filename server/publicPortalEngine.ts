export const portalSections=['overview','carbon','esg','targets','reports','certificates','policies','commitments','suppliers','investors'] as const;
export function normalizePortalSlug(value:unknown){return String(value??'').trim().toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,80)}
export function validPortalSections(value:unknown){const requested=Array.isArray(value)?value.map(String):[];return [...new Set(requested.filter(item=>(portalSections as readonly string[]).includes(item)))]}
export function publicationReadiness(input:{title?:string;slug?:string;sections?:string[];snapshot?:any}){const issues:string[]=[];if(!String(input.title??'').trim())issues.push('Portal title is required.');if(!normalizePortalSlug(input.slug))issues.push('A valid public slug is required.');if(!input.sections?.length)issues.push('At least one public section is required.');if(!input.snapshot?.organization?.name)issues.push('Organization identity is missing from the snapshot.');return{ready:issues.length===0,issues}}
export function aggregateSupplierReadiness(rows:any[]){const total=rows.length,responded=rows.filter(row=>['responded','improvement-plan','verified'].includes(row.engagement_status)).length,verified=rows.filter(row=>row.data_quality==='third-party-verified'||row.engagement_status==='verified').length;return{totalSuppliers:total,respondedSuppliers:responded,verifiedSuppliers:verified,responseRate:total?Math.round(responded/total*100):0,note:'Aggregate supplier engagement only; supplier identities and commercial data are not published.'}}
